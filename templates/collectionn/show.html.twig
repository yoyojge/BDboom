{% extends 'base.html.twig' %}

{% block title %}Collection{% endblock %}

{% block body %}

<main>
<div id="myCarouselInner" >
<div class="bdboomPaddingMain">

    {#  message flash collection ajoutée #}
        {% for message in app.flashes('albumAjout') %}
        <div class="alert alert-success" role="alert">
            {{ message }}
        </div>
        {% endfor %}

    <h1>Collection : {{ collectionn.CollectionName }}</h1>

    {# <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ collectionn.id }}</td>
            </tr>
            <tr>
                <th>CollectionName</th>
                <td>{{ collectionn.CollectionName }}</td>
            </tr>
        </tbody>
    </table> #}




    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Album</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {# {{ dump(ListeAlbumCollection) }} #}
        {% for album in ListeAlbumCollection %}
            <tr>
                <td>{{ album.id }}</td>
                <td>{{ album.title }}</td>
                <td>
                    <a href="{{ path('app_album_show', {'id': album.id , 'idCollection': collectionn.id}) }}"><i class="fa-solid fa-eye"></i></a>
                    <a href="{{ path('app_BDboom_suppItemToCollectionOrWishlist', {'idAlbum': album.id, 'idCollection': collectionn.id}) }}"><i class="fa-solid fa-trash"></i></a>
                    {# <a href="{{ path('app_collectionn_edit', {'id': collectionn.id}) }}">edit</a> #}
                </td>
            </tr>
        {% else %} 
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %} 
        </tbody>
    </table>





    {# <a href="{{ path('app_collectionn_index, { collectionn.id }') }}">back to list</a> #}

    {# <a href="{{ path('app_collectionn_edit', {'id': collectionn.id}) }}">edit</a> #}

    {# {{ include('collectionn/_delete_form.html.twig') }} #}
</div>
</div>

</main>
{% endblock %}
