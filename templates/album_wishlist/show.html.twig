{% extends 'base.html.twig' %}

{% block title %}AlbumWishlist{% endblock %}

{% block body %}
    <h1>AlbumWishlist</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ album_wishlist.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_album_wishlist_index') }}">back to list</a>

    <a href="{{ path('app_album_wishlist_edit', {'id': album_wishlist.id}) }}">edit</a>

    {{ include('album_wishlist/_delete_form.html.twig') }}
{% endblock %}
