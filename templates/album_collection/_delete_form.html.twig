<form method="post" action="{{ path('app_album_collection_delete', {'id': album_collection.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ album_collection.id) }}">
    <button class="btn">Delete</button>
</form>
