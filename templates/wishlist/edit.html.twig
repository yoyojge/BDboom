{% extends 'base.html.twig' %}

{% block title %}Edit Wishlist{% endblock %}

{% block body %}
<main>
 <div  id="myCarouselInner" >
    <div class="bdboomPaddingMain">
        <h1>Renommer la Wishlist : {{wishlist.WishlistName}}</h1>

        {{ include('wishlist/_form.html.twig', {'button_label': 'Renommer'}) }}

        {# <a href="{{ path('app_wishlist_index') }}">back to list</a> #}

        {# {{ include('wishlist/_delete_form.html.twig') }} #}
    </div>
</div>
</main>
{% endblock %}
